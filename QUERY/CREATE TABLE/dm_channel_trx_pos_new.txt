CREATE EXTERNAL TABLE `bd_tableau_dev.dm_channel_trx_pos_new`(
  `channel_type` string, 
  `trx_date` timestamp, 
  `posting_date` timestamp, 
  `ref_no` string, 
  `account_number` string, 
  `product_code` string, 
  `prod_name` string, 
  `lob_code` string, 
  `lob_name` string, 
  `card_number` string, 
  `cif` decimal(13,0), 
  `terminal_id` string, 
  `trx_type` string, 
  `dr_or_cr` string, 
  `mcc_code` string, 
  `mcc` string, 
  `result` string, 
  `no_of_trx` decimal(10,0), 
  `amount` decimal(22,2), 
  `amount_origin` decimal(22,2), 
  `ccy` string, 
  `ccy_rate` decimal(22,2), 
  `user` string, 
  `trx_description` string, 
  `merch_type_channel` string, 
  `cod_ao_business` string, 
  `cod_cust_id` string, 
  `dat_cust_open` timestamp, 
  `cod_cc_homebrn` decimal(5,0), 
  `cif_officer_id` string, 
  `mitra_id` string, 
  `mitra_name` string, 
  `auth_no` decimal(10,0), 
  `trx_origin` string, 
  `country` string, 
  `flag_contactless` string)
PARTITIONED BY ( 
  `date_pr` string)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.serde2.lazy.LazySimpleSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.mapred.TextInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.HiveIgnoreKeyTextOutputFormat'
LOCATION
  'hdfs://DNMUAT/warehouse/tablespace/external/hive/bd_tableau_dev.db/dm_channel_trx_pos_new'
TBLPROPERTIES (
  'bucketing_version'='2', 
  'discover.partitions'='true', 
  'transient_lastDdlTime'='1721732986')